<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.saf9.mobile.find_dining.mapper.UserMapper">
    <select id="getAllUsers" resultType="User">
        SELECT user_id, nick_name, phone, address, get_liked
        FROM user
    </select>

    <select id="getUserById" parameterType="String" resultType="User">
        SELECT user_id, nick_name, phone, address, get_liked
        FROM user
        WHERE user_id = #{user_id}
    </select>

    <insert id="insertUser" parameterType="User">
        INSERT INTO user(nick_name, phone, address, get_liked)
        values (#{nick_name}, #{phone}, #{address}, #{get_liked})
    </insert>

    <update id="updateUser" parameterType="User">
        UPDATE user
        SET nick_name = #{nick_name}, phone = #{phone}, address = #{address}, get_liked = #{get_liked}
        WHERE user_id = #{user_id}
    </update>

    <delete id="deleteUserById" parameterType="String">
        DELETE
        FROM user
        WHERE user_id = #{user_id}
    </delete>


</mapper>
